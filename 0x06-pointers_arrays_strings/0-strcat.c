#include "main.h"

/**
 * *_strcat - concatenates two strings
 *
 * @src: input string
 * @dest: input string
 *
 * return: to dest
*/

char *_strcat(char *dest, char *src)
{
	int i, j;

	i = 0;

	while (s1[i])
		i++;

	for (j = 0; s2[j]; j++)

		s1[i++] = s2[j];

	return (dest);
}
