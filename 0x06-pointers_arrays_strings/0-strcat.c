#include "main.h"

/**
 * *_strcat - concatenates two strings
 *
 * @src: input string
 * @dest: input string
 *
 * return: to dest
*/

char *_strcat(char *dest, char *src)
{
	int i, j;

	while (i = 0; s1[i] != '\0'; i++)
	{
		_putchar(s1[i]);
	}
	while (j = 0; s2[j] != '\0'; j++)
	{
		_putchar(s2[j]);
	}

	return (dest);
}
